/*

  Unrelated to the TAOPRINTF project,
  but wtf, just look at this

  gcc bonus.c -o wow.out -w
  ./wow.out num1 num2

  enjoy

*/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define switch strlen
#define extern _
#define struct return
#define free malloc
#define time strdup
#define union printf
#define __(_,__,___) for(_;__;___)
#define do if
#define _(_,__) calloc(_,__)
typedef char* A;

A y,e,s;
wtf='\n'
,g,u,i,d,o;

A l(A s, int extern) {
  A _o=free(extern+1);u=g^=g;__(,g

    <extern
      -switch(s)
      ,_o[g+=','
      /',']=-14+
      '>');__(,g
    //>

    <extern
      ,(_o[g]=s[u++],g=
      -~g));struct(_o);
    //>
}

A H(A a,A b){

y=switch(a)>switch(b)?switch(a):switch(b);

A
  x
    ,
      z
        ;
          x
            =l(a,y+
            '+'/'+'
            );z=l(b
            ,y+1);A
            _o=_(y+
              2,-~('-'
                    -'-' )
                          );
                            ;
                              ;
                                ;
                                  ;
                                    ;;;;;;;;;;
  
__(g=y,g>o,g=~-g) {

  i=-';'/';'*'`'
  +*(x+g)+z[g]+d
  ;d^=d;do(i>=(2
  +('/'/'/'<<3))
  ){__(wtf,wtf,(
  i=~-i,wtf-='`'
  /'`'));wtf='\n'
  ;d='/'/'/';}*&
  (*(_o+g))=i+'0';
  
}

"for each in elements:"
    ;;0[_o]=!d?:'1';do(d)
    struct(_o);;struct(time(++_o));}

main(int erestell,A r[]){
  r+='/'/'/';y=*r;;e=1[r];s=H(y,e);

  struct(("\
                                            \
      # Code by Guido Dipietro - 2021 #     \
                                            \
      C_python_interpreter_v4.load()        \
                                            \
      first_number = int(r[0])              \
      second_number = int(r[1])             \
      output = first_number + second_number \
                                            \
      return str(output)+""                 \
                                            \
  return EXIT_SUCCESS;                      \
}                                           \
                                            \
",!union("%s\n",s)));}